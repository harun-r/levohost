import React from 'react';

const SectionHeadingBig = props => {
    return (
        <div className="text-center heading-big">
            <h3>{props.heading}</h3>
        </div>
    );
};

export default SectionHeadingBig;
